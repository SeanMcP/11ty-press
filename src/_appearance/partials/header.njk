<header class="site-header">
    <a href="/">{{ metadata.title }}</a>
    <nav>
        {% for link in menu.primary %}
            <a href="{{ link.url }}">{{ link.label }}</a>
        {% endfor %}
    </nav>
</header>